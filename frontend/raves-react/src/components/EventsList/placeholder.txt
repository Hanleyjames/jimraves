create component
call the api project successfully
this info should be available in jws
just focus on get requests

const eventSchema = mongoose.Schema({
  _id: mongoose.Schema.Types.ObjectID,
  artist_ids: [mongoose.Schema.Types.ObjectID],
  eventdatetime:{ type: Date, default: Date.now()},
  venuename: String,
  venuephone: String,
  eventlinks: [String]
});

_id artist_ids eventdatetime venuename <- Get All Events should display this in a list

request: {
              type: 'GET PATCH DELETE',
              eventurl: 'http://'+req.headers.host+'/events'+doc._id
            }

'_id artist_ids eventdatetime venuename venuephone eventlinks' <- get single event